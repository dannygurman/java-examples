package javaVersions.v15.sealed.nonSealed;

public class NonSealedPersonApp {

/*    public int getId (PersonV1 person) {
        if (person instanceof EmployeeV1) {
            return ((EmployeeV1) person).getEmployeeId();
        }
        else if (person instanceof ManagerV1) {
            return ((ManagerV1) person).getManagerId();
        }
        //Without a sealed class, the compiler can't reasonably determine that all possible sub-classes
        are covered with our if-else statements.
       // Without an else clause at the end, the compiler would likely issue a warning
        //indicating our logic doesn't cover every case.
    }*/

}
